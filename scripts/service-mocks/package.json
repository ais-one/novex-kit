{
  "name": "@tools/service-mocks",
  "description": "Mock kafka, redis, and oidc-provider services, and saml-idp for local development and testing.",
  "type": "module",
  "private": true,
  "scripts": {
    "kafka": "echo \"Kafka mock service initialized\"",
    "redis": "echo \"Redis mock service initialized\"",
    "oidc": "echo \"OIDC mock service initialized\"",
    "saml": "echo \"SAML mock service initialized\""
  },
  "dependencies": {
    "@js-ak/kafkajs-mock": "^1.0.0",
    "ioredis-mock": "^8.13.1",
    "kafkajs": "^2.2.4",
    "oidc-provider": "^9.11.3"
  },
  "devDependencies": {
    "saml-idp": "^1.2.1"
  }
}
